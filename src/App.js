import React from 'react';
import TopBar from './components/TopBar';

const App = function (props) {
  return (
    <React.Fragment>
      <TopBar />
    </React.Fragment>
  );
};

export default App;
